import height from './Header';

const vertGap = 10;

const screenHeight: number = window.innerHeight - height;

const longerListSpacing: number = 255 + vertGap;

export { screenHeight, longerListSpacing, vertGap };
